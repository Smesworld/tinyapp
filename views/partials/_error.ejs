<% if (typeof status !== 'undefined') { %>
  <div class="alert alert-error">
    <h1 class="text-danger mx-auto p-3 w-50 rounded bg-warning"><%= status %></h1>
    <h3 class="text-danger mx-auto p-3 w-50 rounded bg-warning"><%= msg %></h3>
  </div>
<% } %>